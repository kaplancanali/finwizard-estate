apiVersion: v1
kind: Service
metadata:
  name: {{ include "property-service.fullname" . }}-api
  labels:
    {{- include "property-service.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.api.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "property-service.apiLabels" . | nindent 4 }}
